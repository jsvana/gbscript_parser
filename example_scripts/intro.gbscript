actor_hide(actorId="player")
await_input(input=["a", "b", "select", "start"])
text(text="New game...")
switch_scene(sceneName="Title Screen", x="0", y="0", direction="", fadeSpeed="2")
if_value_compare(vectorX="12", operator="==", vectorY="12")
if_true()
  text(text="New game...")
  text(foo="bar")
if_false()
  text(text="asdffdsa")
  if_value_compare(foo="bar")
  if_true()
    text(a="asdf")
  if_false()
    text(b="bsdf")
